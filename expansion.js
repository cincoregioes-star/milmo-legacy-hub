(()=>{
const EXP_URL='https://ayfdkemjykhxqirlzbzw.supabase.co';
const EXP_KEY='sb_publishable_jYLFHHgTCQkufeykOAFmmQ_cyroudoy';
const expDb=window.supabase?.createClient?window.supabase.createClient(EXP_URL,EXP_KEY):null;
const q=s=>document.querySelector(s), qa=s=>[...document.querySelectorAll(s)];
const xEsc=(s='')=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const guides={
 'rhubarb-island':{name:'Ilha Ruibarbo',en:'Rhubarb Island',adventure:'Summer Tide Saga',level:5,quests:27,tokens:8,medal:'Diviner',reward:'Cabo de Espada (Sword Hilt)',agent:'Island Explorer',source:'https://milmo.fandom.com/wiki/Rhubarb_Island',accent:'🌿',connections:'Ilha Kraken • Ilha da Casa da Árvore'},
 'swamp-island':{name:'Ilha do Pântano',en:'Swamp Island',adventure:'Summer Tide Saga',level:5,quests:22,tokens:8,medal:'Treasure Finder',reward:'Recompensa de exploração da área',agent:'Mission Agent',source:'https://milmo.fandom.com/pt-br/wiki/Ilha_do_P%C3%A2ntano',accent:'🛸',connections:'Ilha Rosa • Ilha do Canhão • Ilha da Vitória-Régia'}
};
const style=document.createElement('style');style.textContent=`
.guide-launch{margin-top:10px;width:100%}.featured-guides{display:grid;grid-template-columns:1fr 1fr;gap:14px}.featured-guide{position:relative;overflow:hidden;background:linear-gradient(145deg,#0d2d45,#081b2a);border:1px solid var(--line);border-radius:18px;padding:20px}.featured-guide:after{content:'';position:absolute;width:150px;height:150px;border-radius:50%;right:-55px;top:-55px;background:radial-gradient(circle,rgba(66,201,255,.14),transparent 67%)}.featured-guide .bigicon{font-size:38px;display:block;margin-bottom:8px}.guide-facts{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin:13px 0}.guide-fact{background:#061827;border:1px solid var(--line);padding:10px;border-radius:11px}.guide-fact small{display:block;color:var(--muted);font-size:9px}.guide-fact b{font-size:12px;color:var(--gold)}.area-guide-modal .modalbox{width:min(960px,96vw)}.guide-summary{display:grid;grid-template-columns:repeat(4,1fr);gap:9px;margin:12px 0 18px}.guide-summary div{padding:11px;border:1px solid var(--line);border-radius:12px;background:#061827}.guide-summary small{display:block;color:var(--muted);font-size:9px}.guide-summary b{font-size:12px;color:#fff}.guide-cols{display:grid;grid-template-columns:1fr 1fr;gap:14px}.guide-section{border:1px solid var(--line);border-radius:14px;padding:15px;background:#081b2a}.guide-section h3{margin-bottom:11px}.mission-search{display:grid;grid-template-columns:1fr auto;gap:8px;margin-bottom:9px}.mission-search input{width:100%;padding:10px;border-radius:9px;border:1px solid var(--line);background:#061827;color:#fff}.mission-list{display:grid;gap:7px;max-height:310px;overflow:auto}.mission-row{padding:9px 10px;border:1px solid var(--line);border-radius:10px;background:rgba(255,255,255,.02);font-size:11px}.knowledge-row{padding:10px 0;border-bottom:1px solid var(--line)}.knowledge-row:last-child{border-bottom:0}.knowledge-row strong{display:block;color:var(--gold);font-size:12px}.knowledge-row p{font-size:11px;margin:5px 0;color:#c9d9e5}.guide-token-grid{display:grid;grid-template-columns:repeat(8,1fr);gap:5px;margin:9px 0}.guide-token-grid button{border:1px solid var(--line);background:#061827;color:#b9ccda;border-radius:8px;padding:8px 0;font-size:10px}.guide-token-grid button.done{background:rgba(139,237,119,.15);color:var(--green);border-color:rgba(139,237,119,.35)}.validation-note{font-size:10px;color:var(--muted);padding:8px 10px;border-left:3px solid var(--gold);background:rgba(255,213,74,.05)}.mission-hub-results{display:grid;grid-template-columns:repeat(2,1fr);gap:8px}.mission-hub-item{padding:11px;border:1px solid var(--line);border-radius:11px;background:#081c2b}.mission-hub-item small{display:block;color:var(--muted);margin-top:3px}.pedia-audit{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.pedia-audit div{padding:13px;border:1px solid var(--line);border-radius:12px;background:#071a29}.pedia-audit b{display:block;font-size:22px;color:var(--gold)}.pedia-audit span{font-size:10px;color:var(--muted)}
@media(max-width:760px){.featured-guides,.guide-cols,.mission-hub-results{grid-template-columns:1fr}.guide-summary,.guide-facts,.pedia-audit{grid-template-columns:1fr 1fr}.guide-token-grid{grid-template-columns:repeat(4,1fr)}}`;
document.head.appendChild(style);

function ensureGuideUI(){
 const p=q('#pedia'); if(!p||q('#featuredGuidesPanel')) return;
 const panel=document.createElement('section');panel.id='featuredGuidesPanel';panel.className='panel';
 panel.innerHTML=`<div class="section-head"><div><span class="eyebrow">GUIAS VERIFICADOS</span><h2>Ilhas em destaque</h2><p>Conteúdo histórico conferido em fontes do MilMo. Os guias preservam a estrutura do jogo antigo e ajudam no Rebirth quando o conteúdo permanecer equivalente.</p></div><div class="agent-chip" data-agent-exp="Island Explorer"><span class="ico">🗺️</span><div><strong>Island Explorer</strong><small>Explorador de Ilhas</small></div></div></div><div class="featured-guides" id="featuredGuides"></div>`;
 p.appendChild(panel);
 const search=document.createElement('section');search.className='panel';search.id='missionHubPanel';search.innerHTML=`<div class="section-head"><div><span class="eyebrow">MISSÕES</span><h2>Busca rápida de missões</h2><p>Procure pelo nome de uma missão já catalogada.</p></div><div class="agent-chip" data-agent-exp="Mission Agent"><span class="ico">🧭</span><div><strong>Mission Agent</strong><small>Agente de Missões</small></div></div></div><div class="mission-search"><input id="missionHubSearch" placeholder="Ex.: Treco Alienígena, Root of the Problem..."><button class="btn secondary" id="missionHubBtn">Buscar</button></div><div id="missionHubResults" class="mission-hub-results"><p class="muted">Digite o nome de uma missão.</p></div>`;p.appendChild(search);
 const modal=document.createElement('div');modal.id='areaGuideModal';modal.className='modal area-guide-modal';modal.innerHTML=`<div class="modalbox"><div class="section-head"><div><span class="eyebrow" id="guideEyebrow">MILMOPEDIA</span><h2 id="guideTitle">Guia da Ilha</h2><p id="guideSubtitle" class="muted"></p></div><button class="btn ghost" id="closeAreaGuide">Fechar</button></div><div id="guideBody"><p class="muted">Carregando...</p></div></div>`;document.body.appendChild(modal);
 q('#closeAreaGuide').onclick=()=>modal.classList.remove('on');modal.onclick=e=>{if(e.target===modal)modal.classList.remove('on')};
 q('#missionHubBtn').onclick=searchMissions;q('#missionHubSearch').addEventListener('keydown',e=>{if(e.key==='Enter')searchMissions()});
 qa('[data-agent-exp]').forEach(el=>el.onclick=()=>{if(typeof openAgent==='function')openAgent(el.dataset.agentExp)});
 renderFeatured();enhanceAreaCards();
}
function renderFeatured(){const host=q('#featuredGuides');if(!host)return;host.innerHTML=Object.entries(guides).map(([slug,g])=>`<article class="featured-guide"><span class="bigicon">${g.accent}</span><span class="tag gold">${xEsc(g.adventure)}</span><h3>${xEsc(g.name)}</h3><p class="muted">${xEsc(g.en)}</p><div class="guide-facts"><div class="guide-fact"><small>Nível</small><b>${g.level}</b></div><div class="guide-fact"><small>Missões</small><b>${g.quests}</b></div><div class="guide-fact"><small>Tokens</small><b>${g.tokens}</b></div><div class="guide-fact"><small>Medalha</small><b>${xEsc(g.medal)}</b></div></div><button class="btn guide-launch" data-guide="${slug}">Abrir guia completo</button></article>`).join('');qa('[data-guide]').forEach(b=>b.onclick=()=>openAreaGuide(b.dataset.guide))}
function enhanceAreaCards(){const grid=q('#areaGrid');if(!grid)return;const apply=()=>{qa('#areaGrid .area-card').forEach(card=>{if(card.querySelector('.guide-launch'))return;const name=(card.querySelector('h3')?.textContent||'').toLowerCase();const slug=name.includes('rhubarb')||name.includes('ruibarbo')?'rhubarb-island':name.includes('swamp')||name.includes('pântano')||name.includes('pantano')?'swamp-island':null;if(slug){const b=document.createElement('button');b.className='btn secondary guide-launch';b.textContent='Abrir guia completo';b.onclick=()=>openAreaGuide(slug);card.appendChild(b)}})};apply();new MutationObserver(apply).observe(grid,{childList:true,subtree:true})}
function tokenState(slug){try{return JSON.parse(localStorage.getItem('tokens_'+slug)||'[]')}catch{return[]}}
function toggleGuideToken(slug,n){let a=tokenState(slug);a=a.includes(n)?a.filter(x=>x!==n):[...a,n].sort((x,y)=>x-y);localStorage.setItem('tokens_'+slug,JSON.stringify(a));openAreaGuide(slug)}
async function openAreaGuide(slug){const g=guides[slug];if(!g)return;const modal=q('#areaGuideModal');modal.classList.add('on');q('#guideTitle').textContent=`${g.accent} ${g.name}`;q('#guideSubtitle').textContent=`${g.en} • ${g.adventure}`;q('#guideBody').innerHTML='<p class="muted">Carregando dados verificados...</p>';
 let missions=[],knowledge=[];if(expDb){const [m,k]=await Promise.all([expDb.from('missions').select('title,npc,objective,reward,source_url').eq('area_slug',slug).order('title'),expDb.from('knowledge_entries').select('title,content,source_url').eq('section','area:'+slug).eq('status','approved').order('title')]);missions=m.data||[];knowledge=k.data||[]}
 const saved=tokenState(slug);q('#guideBody').innerHTML=`<div class="guide-summary"><div><small>Nível</small><b>${g.level}</b></div><div><small>Missões catalogadas</small><b>${missions.length}/${g.quests}</b></div><div><small>Exploration Tokens</small><b>${saved.length}/${g.tokens}</b></div><div><small>Medalha</small><b>${xEsc(g.medal)}</b></div></div><div class="callout"><b>Conexões:</b> ${xEsc(g.connections)}<br><b>Recompensa de exploração:</b> ${xEsc(g.reward)}</div><div class="guide-cols"><section class="guide-section"><h3>🗺️ Exploration Tokens</h3><div class="guide-token-grid">${Array.from({length:g.tokens},(_,i)=>`<button class="${saved.includes(i+1)?'done':''}" data-guide-token="${i+1}">${i+1}</button>`).join('')}</div><p class="validation-note">Checklist funcional. As posições individuais dos tokens só serão publicadas quando houver validação visual/fonte específica, para evitar indicar local errado.</p><p style="margin-top:11px"><a class="link" href="${g.source}" target="_blank" rel="noopener">Fonte da área ↗</a></p></section><section class="guide-section"><h3>📚 Informações verificadas</h3>${knowledge.length?knowledge.map(k=>`<div class="knowledge-row"><strong>${xEsc(k.title)}</strong><p>${xEsc(k.content)}</p>${k.source_url?`<a class="link" href="${xEsc(k.source_url)}" target="_blank" rel="noopener">Fonte ↗</a>`:''}</div>`).join(''):'<p class="muted">Conteúdo ainda em catalogação.</p>'}</section></div><section class="guide-section" style="margin-top:14px"><h3>🧭 Missões da ilha (${missions.length})</h3><div class="mission-search"><input id="areaMissionSearch" placeholder="Filtrar missões desta ilha"><button class="btn secondary" id="areaAgentBtn">Estou preso</button></div><div class="mission-list" id="areaMissionList">${missionRows(missions)}</div></section>`;
 qa('[data-guide-token]').forEach(b=>b.onclick=()=>toggleGuideToken(slug,Number(b.dataset.guideToken)));const inp=q('#areaMissionSearch');inp.oninput=()=>{const s=inp.value.toLowerCase();q('#areaMissionList').innerHTML=missionRows(missions.filter(m=>m.title.toLowerCase().includes(s)))};q('#areaAgentBtn').onclick=()=>{modal.classList.remove('on');if(typeof openAgent==='function')openAgent(g.agent)};
}
function missionRows(rows){return rows.map(m=>`<div class="mission-row"><b>${xEsc(m.title)}</b>${m.objective?`<div class="muted">${xEsc(m.objective)}</div>`:''}${m.source_url?`<a class="link" href="${xEsc(m.source_url)}" target="_blank" rel="noopener">fonte ↗</a>`:''}</div>`).join('')||'<p class="muted">Nenhuma missão encontrada.</p>'}
async function searchMissions(){const term=q('#missionHubSearch')?.value.trim();const host=q('#missionHubResults');if(!host||!term)return;if(!expDb){host.innerHTML='<p class="error">Banco indisponível.</p>';return}host.innerHTML='<p class="muted">Buscando...</p>';const {data,error}=await expDb.from('missions').select('title,area_slug,npc,objective,reward,source_url').ilike('title','%'+term+'%').limit(30);if(error){host.innerHTML='<p class="error">Erro na busca.</p>';return}host.innerHTML=(data||[]).map(m=>`<article class="mission-hub-item"><b>${xEsc(m.title)}</b><small>${xEsc(guides[m.area_slug]?.name||m.area_slug||'MilMo')}</small>${m.objective?`<p>${xEsc(m.objective)}</p>`:''}${m.source_url?`<a class="link" href="${xEsc(m.source_url)}" target="_blank" rel="noopener">Fonte ↗</a>`:''}</article>`).join('')||'<p class="muted">Nenhuma missão catalogada com esse termo.</p>'}
async function injectAudit(){const admin=q('#admin');if(!admin||q('#pediaAuditPanel'))return;const s=document.createElement('section');s.id='pediaAuditPanel';s.className='panel';s.innerHTML='<span class="eyebrow">MILMOPEDIA</span><h2>Estado da base de conhecimento</h2><div class="pedia-audit" id="pediaAudit"><div><b>...</b><span>Missões</span></div><div><b>...</b><span>Áreas</span></div><div><b>...</b><span>Conhecimento</span></div><div><b>...</b><span>Conquistas</span></div></div>';admin.appendChild(s);if(!expDb)return;const tables=[['missions','Missões'],['milmo_areas','Áreas'],['knowledge_entries','Conhecimento'],['achievement_catalog','Conquistas']];const vals=[];for(const [t,l] of tables){const {count}=await expDb.from(t).select('*',{count:'exact',head:true});vals.push(`<div><b>${Number(count||0).toLocaleString('pt-BR')}</b><span>${l}</span></div>`)}q('#pediaAudit').innerHTML=vals.join('')}
function init(){ensureGuideUI();injectAudit();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();